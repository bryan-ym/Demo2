<%= form_for(@need) do |f| %>
  <% if @need.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@need.errors.count, "error") %> prohibited this need from being saved:</h2>

      <ul>
      <% @need.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h2> 需求澄清环节</h2 >
<hr>
  
  <div class="field">
    <%= f.label "邀请参加评论人员" %><br />
    <%= f.text_field :commentator %>
  </div>
  <div class="field">
    <%= f.label "需求评述" %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label "最终需求" %><br />
    <%= f.text_area :finalreq %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
